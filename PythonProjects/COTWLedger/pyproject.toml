[project]
name = "cotwledger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jaimie Gill", email = "jaimiesgill@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "customtkinter>=6.0.0",
    "pillow>=12.3.0",
]

[project.scripts]
cotwledger = "cotwledger:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
