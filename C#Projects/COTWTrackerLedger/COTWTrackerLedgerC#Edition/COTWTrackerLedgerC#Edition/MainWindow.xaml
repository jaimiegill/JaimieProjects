<Window x:Class="COTWTrackerLedgerC_Edition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:COTWTrackerLedgerC_Edition"
        mc:Ignorable="d"
        Title="COTW Tracker Ledger C# Edition" Height="700" Width="1300">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="SelectReserveLabelStyle">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="SelectReserveComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
    <StackPanel Orientation="Horizontal" Height="Auto" Width="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Select Reserve: " Style="{StaticResource SelectReserveLabelStyle}"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Width="220" Name="ReserveComboBox" SelectionChanged="ReserveComboBox_SelectionChanged" Style="{StaticResource SelectReserveComboBox}"/>
        </Grid>
    </StackPanel>
</Window>