<Window x:Class="COTWTrackerLedgerC_Edition.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:COTWTrackerLedgerC_Edition"
        mc:Ignorable="d"
        Title="COTW Tracker Ledger C# Edition" Height="900" Width="1300">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="SelectReserveLabelStyle">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="SelectReserveComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row 0: Select Reserve -->
            <RowDefinition Height="*"/>
            <!-- Row 1: Main Content -->
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Column 0: Left Sidebar -->
            <ColumnDefinition Width="*"/>
            <!-- Column 1: Main Chart Area -->
        </Grid.ColumnDefinitions>

        <!-- Row 0: Select Reserve -->
        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
            <TextBlock Text="Select Reserve: " Style="{StaticResource SelectReserveLabelStyle}"/>
            <ComboBox Width="220" Name="ReserveComboBox" SelectionChanged="ReserveComboBox_SelectionChanged" Style="{StaticResource SelectReserveComboBox}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Row 1, Column 0: Left Sidebar GroupBox -->
        <GroupBox Grid.Row="1" Grid.Column="0" Header="Species" BorderBrush="#FF828282" BorderThickness="1" Width="200" Margin="0,5,5,0">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="130"/>
                    <!-- Species ListBox -->
                    <RowDefinition Height="Auto"/>
                    <!-- Animals Header Text -->
                    <RowDefinition Height="*"/>
                    <!-- Animal ListBox -->
                </Grid.RowDefinitions>

                <!-- Species List -->
                <ListBox Grid.Row="0" x:Name="SpeciesListBox" Margin="0,0,0,5"/>

                <!-- Animals Header -->
                <TextBlock Grid.Row="1" Text="Animals (high -> low)" FontWeight="Bold" Margin="0,5,0,5"/>

                <!-- Animals List -->
                <ListBox Grid.Row="2" x:Name="AnimalListBox" Margin="0,0,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Row 1, Column 1: Main ScottPlot View -->
        <ScottPlot:WpfPlot Grid.Row="1" Grid.Column="1" x:Name="AnimalPlot" Margin="5,5,0,0"/>
    </Grid>
</Window>